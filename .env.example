# Configuration Backstage AI Studio
# Copier ce fichier vers .env et remplir les valeurs

# === TOKENS D'ACCÈS ===
# Token GitLab - Requis pour découverte automatique des repos
# Générer sur : https://gitlab.com/-/profile/personal_access_tokens
# Scopes requis : read_api, read_repository
GITLAB_TOKEN=glpat-your-gitlab-token-here

# Token GitHub - Optionnel pour intégrations supplémentaires
# Générer sur : https://github.com/settings/tokens
# Scopes requis : repo, read:org
GITHUB_TOKEN=ghp_your-github-token-here

# === SÉCURITÉ ===
# Clé secrète pour le backend - Générer une clé aléatoire forte
BACKEND_SECRET=your-super-secret-backend-key-change-this-in-production

# === BASE DE DONNÉES (Production) ===
# Pour développement, SQLite en mémoire est utilisé
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-postgres-password
POSTGRES_DB=backstage_plugin_catalog

# === KUBERNETES (Optionnel) ===
# Configuration pour intégration Kubernetes
K8S_CLUSTER_URL=https://your-k8s-cluster.com
K8S_SERVICE_ACCOUNT_TOKEN=your-k8s-token
K8S_DASHBOARD_URL=https://your-k8s-dashboard.com

# === TECHDOCS CLOUD STORAGE (Production) ===
# Pour production avec stockage externe
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
TECHDOCS_S3_BUCKET_NAME=ai-studio-techdocs

# Ou Google Cloud Storage
GCS_PROJECT_ID=your-gcp-project
GCS_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GCS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
TECHDOCS_GCS_BUCKET_NAME=ai-studio-techdocs

# === AUTHENTIFICATION (Production) ===
# GitHub OAuth App
AUTH_GITHUB_CLIENT_ID=your-github-oauth-app-id
AUTH_GITHUB_CLIENT_SECRET=your-github-oauth-secret

# GitLab OAuth App
AUTH_GITLAB_CLIENT_ID=your-gitlab-oauth-app-id
AUTH_GITLAB_CLIENT_SECRET=your-gitlab-oauth-secret

# === MONITORING (Optionnel) ===
# Intégrations monitoring
PROMETHEUS_URL=http://localhost:9090
GRAFANA_URL=http://localhost:3000

# === DÉVELOPPEMENT ===
# Ports personnalisés si nécessaire
FRONTEND_PORT=3100
BACKEND_PORT=7007

# Mode debug
DEBUG=false
LOG_LEVEL=info