# Configuration Backstage AI Studio
# Copiez ce fichier en .env.local et remplissez les valeurs

# GitLab Token (requis)
# Créez un Personal Access Token avec les scopes : read_api, read_repository
# https://gitlab.com/-/profile/personal_access_tokens
GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxxx

# Backend Secret (requis pour la production)
# Générez avec : node -p 'require("crypto").randomBytes(24).toString("base64")'
AUTH_BACKEND_SECRET=your-secret-key-here

# GitHub Token (optionnel)
# Si vous avez aussi des repos sur GitHub
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxx

# Azure DevOps Token (optionnel)
AZURE_TOKEN=your-azure-pat

# Bitbucket (optionnel)
BITBUCKET_USERNAME=your-username
BITBUCKET_PASSWORD=your-app-password

# Jenkins (optionnel)
# Format : username:token en base64
JENKINS_AUTH=your-jenkins-auth

# OAuth GitHub (optionnel, pour l'authentification)
AUTH_GITHUB_CLIENT_ID=your-client-id
AUTH_GITHUB_CLIENT_SECRET=your-client-secret